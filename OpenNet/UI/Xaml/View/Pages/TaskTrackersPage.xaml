<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="OpenNet.UI.Xaml.View.Pages.TaskTrackersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Column Headers -->
        <Grid Grid.Row="0"
              Padding="8,4"
              Background="{ThemeResource SystemControlAcrylicWindowBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Uid="TrackerHeader_URL"
                       Grid.Column="0"
                       FontWeight="SemiBold"
                       Text="URL"/>
            <TextBlock x:Uid="TrackerHeader_Tier"
                       Grid.Column="1"
                       FontWeight="SemiBold"
                       Text="Tier"/>
            <TextBlock x:Uid="TrackerHeader_Peers"
                       Grid.Column="2"
                       FontWeight="SemiBold"
                       Text="Peers"/>
            <TextBlock x:Uid="TrackerHeader_Status"
                       Grid.Column="3"
                       FontWeight="SemiBold"
                       Text="Status"/>
            <TextBlock x:Uid="TrackerHeader_Message"
                       Grid.Column="4"
                       FontWeight="SemiBold"
                       Text="Message"/>
        </Grid>

        <!-- Tracker list -->
        <ListView x:Name="TrackersListView"
                  Grid.Row="1"
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="8,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="3*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="2*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Text="{Binding [URL]}"
                                   TextTrimming="CharacterEllipsis"/>
                        <TextBlock Grid.Column="1"
                                Text="{Binding [Tier]}"/>
                        <TextBlock Grid.Column="2"
                                Text="{Binding [Peers]}"/>
                        <TextBlock Grid.Column="3"
                                Text="{Binding [Status]}"/>
                        <TextBlock Grid.Column="4"
                                   Text="{Binding [Message]}"
                                   TextTrimming="CharacterEllipsis"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Empty state -->
        <TextBlock x:Name="EmptyStateText"
                   x:Uid="TrackersList_EmptyState"
                   Grid.Row="1"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   Text="No trackers"
                   Visibility="Collapsed"/>
    </Grid>
</Page>
