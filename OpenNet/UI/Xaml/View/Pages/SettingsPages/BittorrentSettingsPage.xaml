<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="OpenNet.UI.Xaml.View.Pages.SettingsPages.BittorrentSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:XamlToolkit.Labs.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:OpenNet.UI.Xaml.View.Pages.SettingsPages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Header  -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="BitTorrent Advanced Settings" />
            <TextBlock
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="Configure libtorrent session parameters" />
        </StackPanel>

        <!--  Search Box  -->
        <AutoSuggestBox
            x:Name="SearchBox"
            Grid.Row="1"
            Margin="0,0,0,16"
            PlaceholderText="Search Options"
            QueryIcon="Find"
            TextChanged="SearchBox_TextChanged" />

        <!--  Options ListView  -->
        <ListView
            x:Name="OptionsListView"
            Grid.Row="2"
            ItemsSource="{x:Bind AdvancedOptions}"
            SelectionMode="Single">
            <ListView.Header>
                <Border
                    x:Name="HeaderBorder"
                    Padding="8,4,0,4"
                    Background="{ThemeResource SolidBackgroundFillColorTertiaryBrush}"
                    CornerRadius="4">
                    <controls:DataTable ColumnSpacing="16">
                        <controls:DataColumn Content="Name" />
                        <controls:DataColumn CanResize="False" Content="Value" />
                    </controls:DataTable>
                </Border>
            </ListView.Header>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:AdvancedOptionItem">
                    <Grid Padding="12,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="200" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            VerticalAlignment="Center"
                            FontWeight="{x:Bind FontWeight}"
                            Text="{x:Bind Name}" />
                        <TextBlock
                            Grid.Column="1"
                            FontWeight="{x:Bind FontWeight}"
                            Text="{x:Bind Value}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!--  Value Editor  -->
        <Grid Grid.Row="3" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Margin="0,0,8,0"
                VerticalAlignment="Center"
                Text="Value:" />
            <TextBox
                x:Name="ValueTextBox"
                Grid.Column="1"
                PlaceholderText="Select an option to edit" />
            <Button
                Grid.Column="2"
                Margin="8,0,0,0"
                Click="ApplyButton_Click"
                Content="Apply"
                Style="{StaticResource AccentButtonStyle}" />
            <Button
                Grid.Column="3"
                Margin="8,0,0,0"
                Click="ResetButton_Click"
                Content="Reset All" />
        </Grid>
    </Grid>
</Page>
