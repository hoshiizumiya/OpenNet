<?xml version="1.0" encoding="utf-8" ?>
<essential:WindowEx
    x:Class="OpenNet.UI.Xaml.View.Windows.CreateTorrentProgressWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:essential="using:WinUI3Package"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Width="1080"
    Height="480"
    MinWidth="480"
    MinHeight="320"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <essential:MicaBackdropWithFallback>
            <essential:MicaBackdropWithFallback.Fallback>
                <DesktopAcrylicBackdrop />
            </essential:MicaBackdropWithFallback.Fallback>
        </essential:MicaBackdropWithFallback>
    </Window.SystemBackdrop>
    <essential:WindowEx.ContextMenu>
        <essential:ModernStandardWindowContextMenu />
    </essential:WindowEx.ContextMenu>

    <Grid x:Name="RootGrid" Loaded="RootGrid_Loaded">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TitleBar
            x:Name="WindowTitleBar"
            x:Uid="CreateTorrentProgressWindowTitleBar"
            Title="Creating torrent"
            Height="32"
            IsBackButtonVisible="False"
            IsPaneToggleButtonVisible="False"
            Subtitle="Hashing files" />

        <StackPanel
            Grid.Row="1"
            Padding="24"
            Spacing="16">
            <TextBlock
                x:Uid="CreateTorrentProgressDescription"
                Style="{StaticResource SubtitleTextBlockStyle}"
                Text="Creating BitTorrent metadata" />
            <Grid ColumnSpacing="12" RowSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock x:Uid="CreateTorrentProgressSourceLabel" Text="Source" />
                <TextBlock
                    x:Name="SourcePathText"
                    Grid.Column="1"
                    IsTextSelectionEnabled="True"
                    TextTrimming="CharacterEllipsis" />
                <TextBlock
                    x:Uid="CreateTorrentProgressTargetLabel"
                    Grid.Row="1"
                    Text="Target" />
                <TextBlock
                    x:Name="TargetPathText"
                    Grid.Row="1"
                    Grid.Column="1"
                    IsTextSelectionEnabled="True"
                    TextTrimming="CharacterEllipsis" />
            </Grid>
            <essential:ProgressBarEx
                x:Name="CreationProgressBar"
                Height="36"
                HorizontalAlignment="Stretch"
                Background="{ThemeResource ControlFillColorSecondaryBrush}"
                CornerRadius="8"
                Value="0">
                <TextBlock
                    x:Name="ProgressPercentText"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontWeight="SemiBold"
                    Text="0%" />
            </essential:ProgressBarEx>
            <TextBlock
                x:Name="StatusText"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                TextWrapping="Wrap" />
        </StackPanel>

        <Grid
            Grid.Row="2"
            Padding="24,12"
            BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
            BorderThickness="0,1,0,0">
            <StackPanel
                HorizontalAlignment="Right"
                Orientation="Horizontal"
                Spacing="8">
                <Button
                    x:Name="OpenFolderButton"
                    x:Uid="CreateTorrentProgressOpenFolder"
                    Click="OpenFolderButton_Click"
                    Content="Open folder"
                    Visibility="Collapsed" />
                <Button
                    x:Name="CloseButton"
                    x:Uid="CommonClose"
                    Click="CloseButton_Click"
                    Content="Close"
                    Visibility="Collapsed" />
                <Button
                    x:Name="CancelButton"
                    Click="CancelButton_Click"
                    Content="Cancel" />
            </StackPanel>
        </Grid>
    </Grid>
</essential:WindowEx>
