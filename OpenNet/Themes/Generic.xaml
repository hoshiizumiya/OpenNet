<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:anim="using:OpenNet.UI.Xaml.Media.Animations"
    xmlns:helpers="using:OpenNet.Helpers"
    xmlns:local="using:OpenNet">

    <!--<Style TargetType="local:InputActiveDesktopAcrylicBackdrop" >
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:InputActiveDesktopAcrylicBackdrop">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->
    <!--  资源：隐式 Opacity 动画定义  -->
    <anim:ImplicitAnimationSet x:Key="IconOpacityAnim">
        <anim:OpacityAnimation
            From="0"
            To="1"
            Duration="0:0:0.18" />
    </anim:ImplicitAnimationSet>

</ResourceDictionary>
