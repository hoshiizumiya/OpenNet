<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="OpenNet.Pages.TasksPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="using:OpenNet.UI.Xaml.Behaviors"
    xmlns:controls="using:XamlToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:labs="using:XamlToolkit.Labs.WinUI"
    xmlns:local="using:OpenNet.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:speedgraph="using:OpenNet.Controls.SpeedGraph"
    xmlns:vm="using:OpenNet.ViewModels"
    x:Uid="TasksPageRoot"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <!--  SearchBox + CommandBar  -->
        <AutoSuggestBox
            x:Name="SearchBox"
            Width="280"
            Margin="12,6"
            HorizontalAlignment="Left"
            PlaceholderText="Search"
            QueryIcon="Find"
            TextChanged="SearchBox_TextChanged" />
        <CommandBar Grid.Row="0" DefaultLabelPosition="Right">
            <AppBarButton
                x:Name="TaskNewButton"
                x:Uid="TaskNewButton"
                Icon="Add">
                <muxc:AppBarButton.Flyout>
                    <muxc:MenuFlyout Placement="Bottom">
                        <muxc:MenuFlyoutItem
                            x:Name="MenuItemAddFromLink"
                            x:Uid="TaskNewFromUrlButton"
                            Click="MenuItemAddFromLink_ClickAsync"
                            Icon="Link"
                            Text="From Link" />
                        <muxc:MenuFlyoutItem
                            x:Name="MenuItemAddFromFile"
                            x:Uid="TaskNewFromFileButton"
                            Click="MenuItemAddFromFile_ClickAsync"
                            Icon="Document"
                            Text="From File" />
                        <muxc:MenuFlyoutSeparator />
                        <muxc:MenuFlyoutItem
                            x:Name="MenuItemAddFromHttp"
                            x:Uid="TaskNewFromHttpButton"
                            Click="MenuItemAddFromHttp_ClickAsync"
                            Text="HTTP Download">
                            <muxc:MenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xE774;" />
                            </muxc:MenuFlyoutItem.Icon>
                        </muxc:MenuFlyoutItem>
                    </muxc:MenuFlyout>
                </muxc:AppBarButton.Flyout>
            </AppBarButton>
            <AppBarButton
                x:Uid="TaskStartButton"
                Command="{x:Bind ViewModel.StartCommand}"
                Icon="Play" />
            <AppBarButton
                x:Uid="TaskPauseButton"
                Command="{x:Bind ViewModel.PauseCommand}"
                Icon="Pause" />
            <AppBarButton
                x:Uid="TaskDeleteButton"
                Command="{x:Bind ViewModel.DeleteCommand}"
                Icon="Delete" />
            <CommandBar.SecondaryCommands>
                <AppBarButton Icon="Setting" Label="Settings">
                    <AppBarButton.KeyboardAccelerators>
                        <KeyboardAccelerator Key="I" Modifiers="Control" />
                    </AppBarButton.KeyboardAccelerators>
                </AppBarButton>
            </CommandBar.SecondaryCommands>

        </CommandBar>

        <SplitView
            Grid.Row="1"
            DisplayMode="Inline"
            IsPaneOpen="True"
            OpenPaneLength="50">
            <SplitView.Pane>
                <!--  左侧筛选导航 / Left-side filter navigation  -->
                <muxc:NavigationView
                    x:Name="FilterNavView"
                    IsBackButtonVisible="Collapsed"
                    IsPaneToggleButtonVisible="False"
                    IsSettingsVisible="False"
                    PaneDisplayMode="LeftCompact"
                    SelectionChanged="FilterNavView_SelectionChanged">
                    <muxc:NavigationView.MenuItems>
                        <muxc:NavigationViewItem
                            x:Uid="FilterNavViewAllTasks"
                            Icon="ViewAll"
                            IsSelected="True"
                            Tag="AllTasks" />
                        <muxc:NavigationViewItem
                            x:Uid="FilterNavViewDownloading"
                            Icon="Download"
                            Tag="Downloading" />
                        <muxc:NavigationViewItem
                            x:Uid="FilterNavViewCompleted"
                            Icon="Accept"
                            Tag="Completed" />
                        <muxc:NavigationViewItem
                            x:Uid="FilterNavViewFailed"
                            Icon="Cancel"
                            Tag="Failed" />
                    </muxc:NavigationView.MenuItems>
                </muxc:NavigationView>
            </SplitView.Pane>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="2*" />
                </Grid.RowDefinitions>

                <!--  Tasks List  -->
                <ListView
                    x:Name="TasksList"
                    Grid.Row="0"
                    ItemsSource="{x:Bind ViewModel.FilteredTasks, Mode=OneWay}"
                    RightTapped="TasksList_RightTapped"
                    SelectionChanged="TasksList_SelectionChanged"
                    SelectionMode="Single">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Padding" Value="0" />
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.Header>
                        <Border
                            Padding="8,4,0,4"
                            Background="{ThemeResource SolidBackgroundFillColorTertiaryBrush}"
                            CornerRadius="4">
                            <!--<interactivity:Interaction.Behaviors>
                                    <behaviors:StickyHeaderBehavior />
                                </interactivity:Interaction.Behaviors>-->
                            <labs:DataTable ColumnSpacing="16">
                                <labs:DataColumn
                                    x:Name="ColName"
                                    DesiredWidth="3*"
                                    Tag="Name">
                                    <TextBlock x:Uid="Header_TaskName" FontWeight="SemiBold" />
                                </labs:DataColumn>
                                <labs:DataColumn
                                    x:Name="ColSize"
                                    DesiredWidth="*"
                                    Tag="Size">
                                    <TextBlock
                                        x:Uid="Header_Size"
                                        FontWeight="SemiBold"
                                        Text="Size" />
                                </labs:DataColumn>
                                <labs:DataColumn
                                    x:Name="ColProgress"
                                    DesiredWidth="*"
                                    Tag="Progress">
                                    <TextBlock
                                        x:Uid="Header_Progress"
                                        FontWeight="SemiBold"
                                        Text="Progress" />
                                </labs:DataColumn>
                                <labs:DataColumn
                                    x:Name="ColDLRate"
                                    DesiredWidth="*"
                                    Tag="DLRate">
                                    <TextBlock
                                        x:Uid="Header_DLRate"
                                        FontWeight="SemiBold"
                                        Text="DL Rate" />
                                </labs:DataColumn>
                                <labs:DataColumn
                                    x:Name="ColULRate"
                                    DesiredWidth="*"
                                    Tag="ULRate">
                                    <TextBlock
                                        x:Uid="Header_ULRate"
                                        FontWeight="SemiBold"
                                        Text="UL Rate" />
                                </labs:DataColumn>
                                <labs:DataColumn
                                    x:Name="ColRemaining"
                                    DesiredWidth="2*"
                                    Tag="Remaining">
                                    <TextBlock
                                        x:Uid="Header_Remaining"
                                        FontWeight="SemiBold"
                                        Text="Remaining" />
                                </labs:DataColumn>
                                <labs:DataColumn
                                    x:Name="ColAddDate"
                                    DesiredWidth="2*"
                                    Tag="AddDate">
                                    <TextBlock
                                        x:Uid="Header_AddDate"
                                        FontWeight="SemiBold"
                                        Text="Add Date" />
                                </labs:DataColumn>
                            </labs:DataTable>
                        </Border>
                    </ListView.Header>
                    <ListView.ContextFlyout>
                        <MenuFlyout>
                            <MenuFlyoutItem
                                x:Name="MoveTaskMenuItem"
                                x:Uid="MoveTaskMenuItem"
                                Click="MoveTaskMenuItem_Click"
                                Icon="MoveToFolder"
                                Text="Move to..." />
                            <MenuFlyoutItem
                                x:Name="OpenTaskLocationMenuItem"
                                x:Uid="OpenTaskLocationMenuItem"
                                Click="OpenTaskLocationMenuItem_Click"
                                Icon="OpenFile"
                                Text="Open Location" />
                            <MenuFlyoutSeparator />
                            <MenuFlyoutItem
                                x:Name="PropertiesMenuItem"
                                x:Uid="PropertiesMenuItem"
                                Click="PropertiesMenuItem_Click"
                                Icon="More"
                                Text="Properties" />
                        </MenuFlyout>
                    </ListView.ContextFlyout>
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <muxc:ItemsStackPanel Orientation="Vertical" />
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="vm:TaskViewModel">
                            <!--  We set HorizontalAlignment to Left here as we're not stretching our content with a star column  -->
                            <labs:DataRow HorizontalAlignment="Left">
                                <TextBlock Text="{x:Bind Name, Mode=OneWay}" TextTrimming="CharacterEllipsis" />
                                <TextBlock Text="{x:Bind Size, Mode=OneWay}" />
                                <TextBlock Text="{x:Bind Progress, Mode=OneWay}" />
                                <TextBlock Text="{x:Bind DownloadRate, Mode=OneWay}" />
                                <TextBlock Text="{x:Bind UploadRate, Mode=OneWay}" />
                                <TextBlock Text="{x:Bind Remaining, Mode=OneWay}" />
                                <TextBlock Text="{x:Bind AddDate, Mode=OneWay}" />
                            </labs:DataRow>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <!--  上下可调分隔条（XamlToolkit GridSplitter）  -->
                <controls:GridSplitter
                    Grid.Row="2"
                    Height="12"
                    HorizontalAlignment="Stretch"
                    ResizeBehavior="BasedOnAlignment"
                    ResizeDirection="Rows">
                    <controls:GridSplitter.RenderTransform>
                        <TranslateTransform X="2" />
                    </controls:GridSplitter.RenderTransform>

                </controls:GridSplitter>

                <!--  下方多标签 + SpeedGraph  -->
                <muxc:SelectorBar
                    x:Name="Task_SelectBar"
                    Grid.Row="2"
                    SelectionChanged="Task_SelectBar_SelectionChanged">
                    <muxc:SelectorBarItem
                        x:Name="SummaryContent"
                        x:Uid="Task_SummaryContent"
                        IsSelected="True" />
                    <muxc:SelectorBarItem x:Name="PeersList" x:Uid="Task_PeersList" />
                    <muxc:SelectorBarItem x:Name="TrackersList" x:Uid="Task_TrackersList" />
                    <muxc:SelectorBarItem x:Name="FilesList" x:Uid="Task_FilesList" />
                </muxc:SelectorBar>

                <!--  SpeedGraph 嵌入在标签栏右侧  -->
                <!--<speedgraph:SpeedGraph
                        x:Name="TaskSpeedGraph"
                        Grid.Column="1"
                        Height="40"
                        Margin="8,0,0,0"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Center" />-->

                <Frame
                    x:Name="ContentFrame"
                    Grid.Row="3"
                    IsNavigationStackEnabled="False" />

            </Grid>
        </SplitView>
    </Grid>
</Page>
